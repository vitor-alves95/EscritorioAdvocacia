using Microsoft.AspNetCore.Identity;

namespace EscritorioAdvocacia.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Por enquanto, não precisamos de nada extra, mas a estrutura está pronta.
        // Você poderia adicionar: public string NomeCompleto { get; set; }
    }
}