@model EscritorioAdvocacia.Models.Processo

@{
    ViewData["Title"] = "Excluir Processo";
}

<h1 class="text-danger mb-4">Excluir Processo</h1>

<div class="alert alert-danger" role="alert">
    <h3>Tem certeza que deseja **excluir permanentemente** o Processo **@Model.NumeroUnificado**?</h3>
    <p class="mb-0">Esta ação é irreversível e removerá todos os dados do processo: **@Model.Titulo**.</p>
</div>

<div class="card card-body bg-light shadow-sm">

    <h4 class="mb-3">Detalhes do Processo</h4>
    <hr />

    <dl class="row">

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.NumeroUnificado)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.NumeroUnificado)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Titulo)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Titulo)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.StatusAndamento)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.StatusAndamento)</dd>

        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.DataAbertura)</dt>
        <dd class="col-sm-9">@Model.DataAbertura.ToString("dd/MM/yyyy")</dd>

        <dt class="col-sm-3">Cliente</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.Cliente.Nome)</dd>

        <dt class="col-sm-3">Advogado Responsável</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.AdvogadoResponsavel.Nome)</dd>

        <dt class="col-sm-3">Tipo de Processo</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.TipoProcesso.Nome)</dd>

        <dt class="col-sm-3">Vara de Origem</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.VaraOrigem.Nome)</dd>

        <dt class="col-sm-12"><hr /></dt>
        <dt class="col-sm-3">@Html.DisplayNameFor(model => model.DescricaoAndamento)</dt>
        <dd class="col-sm-9">@Html.DisplayFor(model => model.DescricaoAndamento)</dd>

    </dl>

    <form asp-action="Delete" class="mt-4">
        <input type="hidden" asp-for="Id" />

        <button type="submit" value="Delete" class="btn btn-danger me-2">
            <i class="fas fa-trash-alt"></i> Confirmar Exclusão
        </button>

        <a asp-action="Index" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Cancelar e Voltar para a Lista
        </a>
    </form>
</div>